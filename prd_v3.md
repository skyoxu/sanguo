# PRD v3（基于当前仓库能力的延展版）

- 文档日期：2026-03-11
- 基线来源：`CURRENT_STAGE_FOR_BMAD.md`（当前已实现能力）+ `prd_v2.md`（下一阶段玩法目标）
- 状态口径：仅以 `tasks.json` 的 `done` 作为已完成边界
- 目标：在不推翻现有系统的前提下，把 v2 的战役化玩法落到当前项目中

---

## 1. 产品目标（本阶段）

构建“战役模式”主体验：

1. 玩家从“主将选择与开局配置”进入对局。
2. 每轮包含“营地阶段 -> 压力结算（Boss 骰子）-> 棋盘移动与事件/战斗 -> 回营地”。
3. 形成明确的胜负闭环：击败最终 Boss 为胜利；营地建筑体力归零为失败。
4. 全过程保持可解释（HUD/日志/事件说明）与可回放（事件载荷完整）。

---

## 2. 现有能力基线（必须复用）

以下能力已在当前仓库实现，v3 必须优先复用而非重写：

- 主循环与回合推进：`SanguoGameLoopController` + `SanguoTurnManager`
- 经济与倍率解释：`SanguoEconomyManager` + `AppliedMultipliers`
- 事件总线与契约：`Game.Core/Contracts/**` + `EventBus`
- UI 解释链：`HUD` + `HudEventDtoMapper` + `EventExplainService`
- 数据驱动骨架：地图/角色/随机事件/行动卡/战斗 catalog loader
- 质量门禁：`quality_gates.py`（含 smoke、coverage、gdunit、perf、audit）

---

## 3. 与 prd_v2 的冲突裁决（P0 级）

> 不先裁决，后续开发会出现“需求正确但代码打架”的硬冲突。

### 3.1 玩家数量与 AI

- `prd_v2` 提到“先关闭 AI”。
- 当前代码 AI 已深度接入主循环，直接删除会破坏稳定性与测试资产。

裁决：
- v3 战役模式首阶段强制纯玩家局（AI 关闭，不提供开关）。
- AI 模块保留在代码层，仅用于非战役模式与后续阶段，不做物理删除。

### 3.2 胜负条件切换

- `prd_v2` 胜利=击败最终 Boss，失败=营地建筑体力归零。
- 当前系统已有传统回合结束/玩家淘汰路径。

裁决：
- 增加 `RunMode = Campaign`，在该模式下启用新的胜负判定。
- 保留旧判定用于兼容测试和历史玩法，不混用规则。

### 3.3 资源语义映射

`prd_v2` 中的资源名词与当前实现不完全一致，统一映射如下：

- 金币 -> 复用 `Money`
- 轮进度 -> 复用当前回合/日期推进并新增“战役轮数”视图字段
- Boss 压力值/精英进攻数量 -> 新增 campaign 域字段，不复用经济字段
- 锦囊（谋略消耗）-> 新增独立资源池，不挤占行动卡库存

### 3.4 战斗失败语义

- `prd_v2` 描述“战斗失败后继续游戏，不触发重罚”。
- 当前系统已有战斗分支回主循环。

裁决：
- v3 采用“失败继续 + 轻惩罚（可配置）”默认策略。
- 轻惩罚仅影响战役资源，不直接 GameOver。

---

## 4. 版本范围（In Scope）

### 4.1 战役模式入口与开局

1. 新游戏配置增加“模式=战役模式”。
2. 开局选择：主将、1 主动谋略（2选1）、1 被动谋略（2选1）、地图。
3. 首发仅开放 1 个默认主将（刘备），其余角色支持“锁定/待开放”状态。

验收要点：
- 开局事件中包含主将、谋略、地图、seed。
- HUD 可展示本局战役模式关键参数。

### 4.2 回合循环（营地 -> 压力 -> 棋盘）

每轮顺序固定：

1. 到达营地时立即结算上一轮随机任务；随后新轮开始并递增 `RoundNumber`。
2. 营地阶段：优先结算营地体力与失败条件，再执行营地自由操作。
3. 离开营地时按规则触发 Boss 流程（揭示/强制挑战）；Boss 结算后发布本轮随机任务。
4. 棋盘阶段：移动、触发格子、战斗/事件/购买等；本轮随机任务在该阶段内有效。

验收要点：
- 顺序稳定且可在日志中完整回放。
- 同一轮内事件顺序确定，不允许乱序触发。
- 若 Boss 战直接结束对局，本轮不发布新随机任务。

### 4.3 营地系统（最小可玩版）

1. 营地有 5 个建筑位，每个建筑有体力。
2. 每轮可激活 1 个营地功能。
3. 建筑体力为 0 时对应功能禁用。
4. 建筑体力归零触发失败事件。

首版建筑功能（最小集）：
- 宝物工坊：获得宝物或金币补偿
- 酒馆：武将成长或金币补偿
- 城防中心：修复营地体力
- 作战大厅：降低下轮 Boss 骰子数量（有下限）
- 训练场：队伍成长增益

### 4.4 Boss 骰子压力系统

1. 按轮数投掷 Boss 骰子（可配置上限）。
2. 产出两类结果：
   - 本轮精英进攻数量
   - 全局 debuff/压力效果
3. 普通难度目标 45-60 分钟（2 个 Boss）；困难/地狱目标 60-90 分钟（3 个 Boss）。
4. Boss 揭示后可跨轮延迟挑战，但延迟会持续增压，达到硬上限后在离开营地时强制挑战。

验收要点：
- 压力结果有来源、有数值、有持续时长。
- HUD 与事件日志可解释每条压力条目。

### 4.5 棋盘与战斗扩展

1. 复用现有 tile 类型（city/event/facility/wild/empty/pass），扩展语义而非改协议。
2. 事件类节点自动触发；`Skip` 仅在“被规则阻止”场景允许，不作为常规策略。
3. 失败后保留对局连续性，回到主循环。

### 4.6 随机任务（最小闭环）

1. 不做任务链；每轮发布 1 条随机任务（首版目标类型：本轮到达营地）。
2. 到达营地即结算上一轮任务（完成给奖励，未完成无惩罚）。
3. 若离开营地触发 Boss 挑战，则 Boss 结算后再发布本轮任务；若对局结束则不发布。

---

## 5. 本阶段不做（Out of Scope）

1. 全量武将池与完整平衡。
2. 多 Boss 同局完整内容包（仅保留接口与配置位）。
3. 全新战斗引擎重写（只在现有战斗分支上扩展）。
4. 破坏现有 done 任务行为的兼容性重构。

---

## 6. 架构与数据约束（必须遵守）

### 6.1 事件契约

- 新增事件必须遵循 ADR-0004 命名规范：`core.*.*`。
- 契约定义进入 `Game.Core/Contracts/**`，UI 仅消费 DTO，不直接拼 JSON。
- 所有关键事件应带：`CorrelationId`、`CausationId`、`RoundNumber`（如适用）。

### 6.2 数据驱动

- 战役新增内容（主将、谋略、营地建筑、Boss 骰子面、任务定义）必须进入数据文件。
- 禁止把规则硬编码在 UI 层。
- 必须经过 catalog/manifest/version 门禁。
- 存档/读档必须校验 `packId/version/content_fingerprint`，不兼容时硬拒绝并产生日志审计事件。

### 6.3 可解释性

- 所有状态变化（金币、体力、压力、战斗结果、任务进度）必须可在 HUD/日志中解释。
- i18n 缺失时允许 fallback，但 CI 中不得长期存在缺失键。

---

## 7. 非功能与质量门禁

1. 保持现有 `quality_gates.py` 全链路可跑。
2. 新增逻辑必须具备：
   - Core 单测（xUnit）
   - 关键 UI/Godot 测试（GdUnit）
3. 覆盖率门禁沿用现有标准（lines >= 90, branches >= 85）。
4. headless smoke 与审计日志不得退化。

---

## 8. 分阶段交付（建议拆分）

### M1（规则框架）

- 战役模式入口
- 新胜负条件
- 回合顺序固定化
- Boss 骰子最小可用

### M2（营地与队伍）

- 营地建筑体力与功能最小集
- 主将 + 谋略选择
- 战役资源池接入 HUD

### M3（随机任务与压力）

- 每轮随机任务发布/结算闭环
- 压力效果多样化
- 解释链与日志完整化

### M4（内容扩展与收口）

- 主将/地图/事件内容扩展
- 数值护栏
- 可玩性回归与平衡修正

---

## 9. 验收标准（可执行）

### 9.1 业务验收

1. 玩家可在战役模式下完成“开局 -> 多轮循环 -> 胜/负结算”。
2. 营地体力归零时触发失败，不发生死锁。
3. 最终 Boss 击败后触发胜利结算与结果界面。

### 9.2 技术验收

1. 新事件契约通过命名与回链校验。
2. quality gates 全绿（或按既有 soft/hard 规则通过）。
3. 无新增 P0/P1 级别回归。

### 9.3 解释性验收

1. 关键行为均有日志条目：触发源、时机、影响字段。
2. HUD 文案中英文可显示，不出现裸事件码。

---

## 10. 测试路线（基于当前资产延展）

### 10.1 复用现有测试资产

- 回合与主循环：`Task17TurnTests`、`test_sanguo_game_loop_dice_flow.gd`
- 经济与倍率：`Task14/15/16` + `Task51AppliedMultipliersPayloadTests`
- 开局配置：`Task50GameStartConfigTests` + `Task54` UI 测试
- 事件顺序：`Task52TurnPhaseWindowTests`
- 随机事件与战斗：`Task56/59`

### 10.2 v3 新增测试主题

1. 战役胜负判定测试（Boss 击败/营地归零）。
2. Boss 骰子压力结算测试（轮数、上限、效果生效周期）。
3. 营地建筑功能/损毁测试。
4. 主将谋略选择与消耗规则测试。
5. 随机任务时序测试（营地结算、Boss 后发布、对局结束不发布）。
6. 战役日志解释测试（来源、时机、影响字段完整）。

---

## 11. 风险与止损

### P0 风险

1. 同时混用旧胜负规则与战役规则导致状态机冲突。
2. 在 UI 层硬编码规则导致后续数据驱动失效。

止损措施：
- 先引入 `RunMode` 并对规则分流。
- 所有规则入 Core + Contracts，UI 只渲染与交互。

### P1 风险

1. 事件字段膨胀但无统一 DTO 映射，解释链不稳定。
2. 新资源（锦囊/营地体力/压力值）与现有经济字段混用。

止损措施：
- 新增资源单独建模，不复用 Money 字段承载非货币语义。
- 事件字段先定义契约，再实现。

---

## 12. 对 BMAD 下一步的输入

1. 以本 PRD 为源，优先拆解 M1 与 M2 任务。
2. 每个任务必须附带：
   - 对应契约文件
   - 对应测试文件（Core + Godot）
   - 事件解释链接入点
3. 任务拆解时明确标注“复用现有实现点”，禁止平行新系统。
